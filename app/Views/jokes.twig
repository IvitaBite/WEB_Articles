<!DOCTYPE html>
<html>
<head>
    <title>Page 2</title>
    <link rel="stylesheet" href="stylesindex.css">
</head>
<body>
<a href="/articles">Back to ....</a>
<h1>Joke Category: {{ jokes.getType() }}</h1>
<div class="container text-center">
    <p class="setup"><strong>Setup:</strong> {{ jokes.getSetup() }}</p>
    <p class="punchline"><strong>Punchline:</strong> {{ jokes.getPunchline() }}</p>
</div>
<div class="bottom">
    <p>Date: {{ currentTime.format('d F Y') }}</p>
    <p>Time: {{ currentTime.format('H:i') }}</p>
</div>
</body>
</html>
